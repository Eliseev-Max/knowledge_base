Configuration file '/etc/default/jenkins'
 ==> Modified (by you or by a script) since installation.
 ==> Package distributor has shipped an updated version.
   What would you like to do about it ?  Your options are:
    Y or I  : install the package maintainer's version
    N or O  : keep your currently-installed version
      D     : show the differences between the versions
      Z     : start a shell to examine the situation
 The default action is to keep your current version.
*** jenkins (Y/I/N/O/D/Z) [default=N] ? D
--- /etc/default/jenkins        2021-06-29 17:31:13.781871735 +0500
+++ /etc/default/jenkins.dpkg-new       2022-09-07 15:06:34.000000000 +0500
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOC


ult to jenkins)















nkins.war --help

lt 0.0.0.0)
disable with -1)

 ESCOD
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOD
+++ /etc/default/jenkins.dpkg-new       2022-09-07 15:06:34.000000000 +0500
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOC


ult to jenkins)















nkins.war --help

lt 0.0.0.0)
disable with -1)

 ESCOD
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOC


ult to jenkins)















nkins.war --help

lt 0.0.0.0)
disable with -1)

 ESCOD
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOC


ult to jenkins)















nkins.war --help

lt 0.0.0.0)
disable with -1)

 ESCOD
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOD
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
 ESCOD
@@ -17,10 +17,10 @@

 # user and group to be invoked as (default to jenkins)
 JENKINS_USER=$NAME
-JENKINS_GROUP=testers
+JENKINS_GROUP=$NAME

 # location of the jenkins war file
-JENKINS_WAR=/usr/share/$NAME/$NAME.war
+JENKINS_WAR=/usr/share/java/$NAME.war

 # jenkins home location
 JENKINS_HOME=/var/lib/$NAME
@@ -67,7 +67,8 @@
 PREFIX=/$NAME

 # arguments to pass to jenkins.
-# --javahome=$JAVA_HOME
+# full list available from java -jar jenkins.war --help
+# --javaHome=$JAVA_HOME
 # --httpListenAddress=$HTTP_HOST (default 0.0.0.0)
 # --httpPort=$HTTP_PORT (default 8080; disable with -1)
 # --httpsPort=$HTTP_PORT
