Сравнение файлов. Утилита diff

[sources:]
  1) "diff(1) - Linux man page": https://linux.die.net/man/1/diff
  2) "Сравнение файлов в Linux": https://losst.pro/sravnenie-fajlov-v-linux
  3) "diff Command": https://www.ibm.com/docs/ru/aix/7.2?topic=d-diff-command
     + https://www.ibm.com/docs/ru/aix/7.2?topic=files-comparing-diff-command


########
# diff #
########

  = построчное сравнение файлов

[Синтаксис:]

diff [OPTION]... FILES

$ diff опции файл1 файл2

[Опции:]

  -q, --brief - выводить только отличия файлов;
  -s, --report-identical-files
    = сообщить о том, что два файла одинаковы

  -с - выводить нужное количество строк после совпадений;
  -u - выводить только нужное количество строк после отличий;
  -y - выводить в две колонки;
  -e - вывод в формате ed скрипта;
  -n - вывод в формате RCS;
  -a - сравнивать файлы как текстовые, даже если они не текстовые;
  -t - заменить табуляции на пробелы в выводе;
  -l, --paginate
    = разделить на страницы и добавить поддержку листания
      (пропустите вывод через 'pr', чтобы поместить его в пагинацию);

  -r, --recursive
    = рекурсивное сравнение папок;
  -i, --ignore-case
    = игнорировать различия в регистре в содержимом файлов;
  -E - игнорировать изменения в табуляциях;
  -Z - не учитывать пробелы в конце строки;
  -b - не учитывать пробелы;
  -B - не учитывать пустые строки.
  